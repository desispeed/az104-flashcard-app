{
  "name": "container-monitor-server",
  "version": "1.0.0",
  "description": "Backend agent that queries Docker container status across hosts over SSH contexts and serves it as token-authed JSON for the ContainerStatus iOS app.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
