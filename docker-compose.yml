services:
  daily-summary-bot:
    build: .
    container_name: daily-summary-bot
    restart: unless-stopped
    env_file: .env
    network_mode: host  # Required for Sonos discovery (SSDP multicast)
